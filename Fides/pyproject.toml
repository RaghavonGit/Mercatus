[project]
name = "fides"
version = "0.1.0"
description = "Fides - the tamper-evident trust ledger of the Mercatus project"
readme = "README.md"
authors = [{ name = "RaghavonGit", email = "raghavnagalingaayyanar@gmail.com" }]
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = ["pip-audit>=2.10.1", "pytest>=9.1.1"]

[tool.pytest.ini_options]
testpaths = ["tests"]
